const section = (path) => path.split("/")[0];
export const editOnlineCds = (code, path) => {};

export const editOnlineCdp = (code, path) => {
  const data = {
    title: `Choc UI ${path} Pen`,
    description: `Edit Choc UI ${path} Code`,
    editors: "001",
  };
};
